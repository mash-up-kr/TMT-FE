/**
 * 검색 결과 자리가 가질 수 있는 상태.
 *
 * "결과 없음"은 여기에 없다. 매장 검색은 결과가 0건이어도 직접 입력 옵션이 남아
 * 빈 화면이 되지 않지만 주소 검색은 빈 화면이 된다. 무엇을 비었다고 볼지는
 * 시트마다 다르므로 각 시트가 판정한다.
 */
export type SearchStatus = "idle" | "loading" | "error" | "ready";
