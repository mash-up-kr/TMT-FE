/**
 * 이 화면만 쓰는 일러스트 색이다. semantic 토큰으로 표현되지 않아 여기 둔다.
 * 값은 시안 배경 이미지(Figma 1792:38179 「image 110808」)를 세로로 샘플링해 옮긴 것이다.
 */
export const SCREEN_BACKGROUND =
  "linear-gradient(180deg, #ffd6dc 0%, #ffe5e9 20%, #fff4f6 40%, #ffffff 55%, #fff2f2 70%, #ffe7eb 80%, #ffd4da 100%)";

/** 냄비 아래 매장 그리드 판의 색. 시안 값 그대로다. */
export const GRID_SURFACE = "#ffd38e";

/**
 * 담기 화면의 세로 치수. 시안(Figma 1674:61058)에서 잰 값이다.
 *
 * 제목·냄비·버튼·그리드를 **한 덩이로 묶어 화면 가운데에 세운다.** 간격은 시안 간격
 * 그대로이고, 세로로 남는 공간은 위아래로 똑같이 나뉜다. 예전처럼 space-between으로
 * 벌리면 화면 높이에 따라 간격이 제각각이 되어 시안과 어긋난다.
 *
 * 블록 전체는 550px다 — 제목 윗변(140)에서 그리드 아랫변(690)까지.
 */
export const LAYOUT = {
  /** 냄비 그림 가로 크기. 시안 Pot 인스턴스(170)와 같은 배율이다. */
  potSize: 170,
  /** 제목 아랫변 → 냄비 그림 윗변. 시안 168 → 196.7. */
  titleToPot: 28,
  /** 냄비 그림 아랫변 → 버튼 윗변. 시안 277.7 → 306. */
  potToButton: 28,
  /** 버튼 아랫변 → 그리드 판 윗변. 시안 350 → 370. */
  buttonToGrid: 20,
  /** 화면 위아래 최소 여백. 세로가 모자라면 여기까지 줄고 그 아래로는 스크롤한다. */
  edgeInset: 24,
} as const;

/**
 * 결과 화면의 세로 치수. 시안(Figma 1674:61096)에서 잰 값이다.
 *
 * 냄비 박스(44~194)는 아래쪽 33px이 빈 공간이라, 카드가 그만큼 물고 올라가야 카드 윗변이
 * 냄비 그림 아랫변에 맞물린다. 시안도 카드가 160에서 시작해 냄비를 34px 덮는다.
 */
export const RESULT_LAYOUT = {
  potSize: 150,
  cardOverlap: 34,
  /** 카드 아랫변 → 버튼 윗변. 시안 535 → 567(버튼 스택 555 + pt 12). */
  cardToButton: 32,
} as const;
